The code for assignment 4 was written in Jupyter Notebook.  

The data source is slightly modified from the original file as the headers contained special characters.

The data file shoul dbe in the same folder as the downloaded jupyter notebook file.

You should be able to select "Restart & Run All" from the Kernel drop down and have the code execute.  